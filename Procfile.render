web: bundle exec puma -t 5:5 -p ${PORT:-3000} -e ${RACK_ENV:-development}
webpack: if [ "$RAILS_ENV" = "development" ]; then ./bin/webpack-dev-server; else echo "Skipping webpack-dev-server in $RAILS_ENV environment"; fi
